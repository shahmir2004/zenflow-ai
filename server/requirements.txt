# ZenFlow AI yoga API.
#
# Deliberately short. The upstream backend also pulls scipy (HMM + Kalman),
# aiofiles and python-multipart (chunked video upload) and supabase — none of
# which the yoga pipeline touches. Dropping them is most of why this server
# boots quickly enough to survive a free-tier cold start.

# Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# Geometry
numpy>=1.26.0

# Config
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Testing
pytest>=7.4
pytest-asyncio>=0.21
httpx>=0.26.0
