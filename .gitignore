# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Node / Next.js  (web/ has its own .gitignore too)
node_modules/
.next/
out/
*.tsbuildinfo

# Env
.env
.env.local
.env*.local

# Editors / OS
.DS_Store
.idea/
.vscode/

# Deploy
.vercel
.gstack/

# OAuth client secret files downloaded from Google Cloud Console.
# These carry a live client secret. GitHub's push protection caught one that a
# `git add -A` swept in from the repo root; this stops the next one.
client_secret*.json
*-credentials.json
